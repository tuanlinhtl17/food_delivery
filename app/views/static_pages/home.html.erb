<% if logged_in? && current_user.shipper? %>
  <%= render "employee_home_page" %>
<% elsif logged_in? && current_user.admin?%>
  <%= render "admin_home_page" %>
<% else %>
  <%= render "customer_home_page" %>
<% end %>
